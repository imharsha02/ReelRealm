export type User = {
    id:string,
    name:string,
    email:string,
    password:string
}

export type Show = {
    show_id:string,
    name:string,
    episodes:number,
    imageSrc:string
}

export type Movie = {
    id:string,
    name:string,
    imageSrc:string
}